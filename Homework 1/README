Best method: Run RUNME.sh script.

Manual method: Compile [problem].cpp files as [problem]. To generate plots simply run the ./[problem].sh file. This runs the executable and generates the plots. All plots are generated as .png files.
